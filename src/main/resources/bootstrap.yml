#端口号
server:
  port: 9999
#数据库
spring:
  application:
    name: myblog-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #文件注册中心地址
        file-extension: yaml #文件扩展格式

profiles:
  active: pro